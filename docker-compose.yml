services:
  app:
    build: .                    # 현재 폴더의 Dockerfile로 이미지를 빌드
    image: hochan00/agent-project:latest
    ports:
      - "8000:8000"             # 호스트 8000 → 컨테이너 8000 연결 (localhost:8000 접속용)
    env_file:
      - .env                    # .env의 비밀키를 컨테이너에 환경변수로 주입 (이미지엔 안 구움)

